class Solution {
    public void reverseString(char[] s) {
        char a;
        int left = 0;
        int right = s.length - 1;
        while(right > left){
            a = s[left];
            s[left] = s[right];
            s[right] = a;
            right--;
            left++;
        }
        
    }
}
